syntax = "proto3";
package usp;

message Get {
  repeated string param_paths = 1;
}

message Set {
  message ParamSetting {
    string param = 1;
    string value = 2;
  }
  repeated ParamSetting param_settings = 1;
}

message Message {
  oneof body {
    Get get = 1;
    Set set = 2;
  }
}
